Inline: $x^2 + y^2 = z^2$, next $E = mc^2$.

Edge: \$20 and \$30 are literals; $a$b$ closes early.

Display alone:

$$
\int_0^\infty e^{-x^2} dx
$$

Mixed inline $a$ with display $$b$$ on the same line.
